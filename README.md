Implements the rough signature kernel from the paper https://arxiv.org/pdf/2404.02926. Depends on JAX and RoughPy-JAX. Created for Datasig.

`example.ipynb` provides a simple application of the kernel method to finding hidden messages in Brownian streams. The implementation of the kernel itself is primarily contained in the file `solver.py`.
